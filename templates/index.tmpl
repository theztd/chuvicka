<!DOCTYPE html>
<html lang="en-US">
  <head>
    <meta charset="utf-8">
    <title>Uzivatelsky panel</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="icon" href="/assets/favicon.ico" type="image/x-icon">
    <link rel="stylesheet" href="/assets/bootstrap.min.css">
    <link rel="stylesheet" href="/assets/main.css">
    <link rel="stylesheet" href="/assets/status_bar.css">
  </head>

  <!-- Dark / Light theme -->
  <body data-bs-theme="dark">
  
  
    <div class="container">
      <nav>
      </nav>

      <main>
        <h1>Seznam monitorovanych url</h1>
        <table class="table">
          <thead>
            <tr class="table-head">
              <th>Url</th>
              <th></th>
              <th>Akce</th>
            </tr>
          </thead>

          <tbody id="tables">
          </tbody>

        </table>

        <h2>Přidat url</h2>
        <form id="add-table-form" class="form">
          <label for="table-name form-label">Název tabulky:</label><br>
          <input type="text" class="form-control" id="table-name" name="table-name"><br>
          <button type="submit" class="btn btn-primary">Přidat</button>
        </form>

      </main>
    </div>

    <script src="/assets/api.js">
    </script>
    <script src="/assets/bootstrap.bundle.min.js">
    </script>

  </body>
</html>